---

---

<section id="how-it-works" class="section how-it-works">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Как это работает</h2>
      <p class="section-subtitle">Четыре простых шага к вашему AI-решению</p>
    </div>
    
    <div class="steps">
      <div class="step">
        <div class="step-number">1</div>
        <div class="step-content">
          <h3 class="step-title">Подключите нужные модели</h3>
          <p class="step-description">Выберите из 50+ передовых нейросетей: GPT-4, Claude, Gemini и другие</p>
        </div>
      </div>
      
      <div class="step">
        <div class="step-number">2</div>
        <div class="step-content">
          <h3 class="step-title">Соберите ИИ-агентов под задачи</h3>
          <p class="step-description">Используйте конструктор без кода для создания специализированных агентов</p>
        </div>
      </div>
      
      <div class="step">
        <div class="step-number">3</div>
        <div class="step-content">
          <h3 class="step-title">Запустите в облаке или по API</h3>
          <p class="step-description">Мгновенное развертывание с REST API, вебхуками и SDK</p>
        </div>
      </div>
      
      <div class="step">
        <div class="step-number">4</div>
        <div class="step-content">
          <h3 class="step-title">Масштабируйте по мере роста</h3>
          <p class="step-description">Гибкие тарифы и квоты, которые растут вместе с вашим бизнесом</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .how-it-works {
    background: #f9fafb;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-title {
    margin-bottom: 1rem;
  }

  .section-subtitle {
    font-size: 1.125rem;
    color: var(--gray);
  }

  .steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    position: relative;
  }

  @media (min-width: 768px) {
    .steps {
      grid-template-columns: repeat(4, 1fr);
    }

    .steps::before {
      content: '';
      position: absolute;
      top: 2rem;
      left: 3rem;
      right: 3rem;
      height: 2px;
      background: linear-gradient(90deg, 
        var(--primary) 0%, 
        var(--primary) 25%, 
        var(--secondary) 50%, 
        var(--accent) 75%, 
        var(--accent) 100%
      );
      z-index: 0;
    }
  }

  .step {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .step-number {
    width: 4rem;
    height: 4rem;
    background: var(--white);
    border: 3px solid var(--primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
  }

  .step:nth-child(2) .step-number {
    border-color: var(--secondary);
    color: var(--secondary);
  }

  .step:nth-child(3) .step-number {
    border-color: var(--accent);
    color: var(--accent);
  }

  .step:nth-child(4) .step-number {
    border-color: var(--primary);
    color: var(--primary);
  }

  .step:hover .step-number {
    transform: scale(1.1);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .step-content {
    max-width: 280px;
  }

  .step-title {
    font-size: 1.125rem;
    margin-bottom: 0.75rem;
    color: var(--dark);
  }

  .step-description {
    color: var(--gray);
    line-height: 1.6;
  }

  @media (max-width: 767px) {
    .steps {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }
</style>