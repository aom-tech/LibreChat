---

---

<section class="hero section">
  <div class="container">
    <div class="hero-content">
      <div class="hero-badge">
        <span>⚡ Без кода</span>
        <span>⚙️ API и облако</span>
        <span>💳 Оплата картой РФ</span>
      </div>
      
      <h1 class="hero-title">
        Единая AI-платформа для бизнеса и авторов. 
        <span class="text-gradient">Оплата картой РФ.</span>
      </h1>
      
      <p class="hero-subtitle">
        Доступ к передовым нейросетям, запуск ИИ-агентов и коробочное решение для создания курсов за минуты.
      </p>
      
      <div class="hero-actions">
        <a href="#lead-form" class="btn btn-primary btn-large" data-form-type="b2c">Попробовать бесплатно</a>
        <a href="#lead-form" class="btn btn-secondary btn-large" data-form-type="b2b">Запросить демо</a>
      </div>
      
      <div class="hero-stats">
        <div class="stat">
          <div class="stat-value">50+</div>
          <div class="stat-label">AI моделей</div>
        </div>
        <div class="stat">
          <div class="stat-value">Без VPN</div>
          <div class="stat-label">Работает в РФ</div>
        </div>
        <div class="stat">
          <div class="stat-value">24/7</div>
          <div class="stat-label">Поддержка</div>
        </div>
      </div>
    </div>
    
    <div class="hero-visual">
      <div class="visual-placeholder">
        <div class="chat-container">
          <div class="chat-header">
            <div class="chat-models">
              <span class="model-badge active">GPT-4</span>
              <span class="model-badge">Claude 3</span>
              <span class="model-badge">Gemini</span>
            </div>
            <span class="chat-status">
              <span class="status-dot"></span>
              Онлайн
            </span>
          </div>
          <div class="chat-messages">
            <div class="message user">
              <div class="message-avatar">👤</div>
              <div class="message-content">
                <p>Создай план онлайн-курса по AI для бизнеса</p>
              </div>
            </div>
            <div class="message assistant">
              <div class="message-avatar">🤖</div>
              <div class="message-content">
                <p>Создаю структуру курса "AI для автоматизации бизнеса":</p>
                <ul class="course-list">
                  <li>✅ Модуль 1: Основы искусственного интеллекта</li>
                  <li>✅ Модуль 2: ChatGPT для бизнес-процессов</li>
                  <li>✅ Модуль 3: Автоматизация маркетинга</li>
                  <li class="typing">Модуль 4: Аналитика данных<span class="cursor">|</span></li>
                </ul>
              </div>
            </div>
          </div>
          <div class="chat-input">
            <input type="text" placeholder="Напишите запрос..." disabled />
            <button class="send-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="22" y1="2" x2="11" y2="13"></line>
                <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
              </svg>
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(180deg, #f9fafb 0%, rgba(249, 250, 251, 0) 100%);
    overflow: hidden;
    position: relative;
  }

  .hero-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero-badge {
    display: inline-flex;
    gap: 1rem;
    padding: 0.5rem 1rem;
    background: var(--white);
    border: 1px solid #e5e7eb;
    border-radius: 2rem;
    margin-bottom: 2rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--gray);
  }

  .hero-badge span {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .hero-title {
    margin-bottom: 1.5rem;
    font-weight: 800;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--gray);
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 3rem;
  }

  .btn-large {
    padding: 1rem 2rem;
    font-size: 1.125rem;
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    padding: 2rem;
    background: var(--white);
    border-radius: 1rem;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
    margin-top: 3rem;
  }

  .stat {
    text-align: center;
  }

  .stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--primary);
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--gray);
    margin-top: 0.25rem;
  }

  .hero-visual {
    margin-top: 4rem;
    position: relative;
  }

  .visual-placeholder {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 1rem;
    padding: 2rem;
    min-height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .chat-container {
    background: var(--white);
    border-radius: 0.75rem;
    width: 100%;
    max-width: 500px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    overflow: hidden;
  }

  .chat-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 1.5rem;
    border-bottom: 1px solid #e5e7eb;
    background: #fafafa;
  }

  .chat-models {
    display: flex;
    gap: 0.5rem;
  }

  .model-badge {
    padding: 0.25rem 0.5rem;
    border-radius: 0.375rem;
    font-size: 0.75rem;
    font-weight: 600;
    background: #e5e7eb;
    color: var(--gray);
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .model-badge.active {
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: var(--white);
  }

  .chat-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: var(--gray);
  }

  .status-dot {
    width: 8px;
    height: 8px;
    background: var(--secondary);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(1.2); }
  }

  .chat-messages {
    padding: 1.5rem;
    min-height: 280px;
    max-height: 280px;
    overflow-y: auto;
    background: var(--white);
  }

  .message {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
    animation: fadeInUp 0.5s ease-out;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .message-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.125rem;
    flex-shrink: 0;
  }

  .message.user .message-avatar {
    background: #f3f4f6;
  }

  .message.assistant .message-avatar {
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.1) 0%, rgba(16, 185, 129, 0.1) 100%);
  }

  .message-content {
    flex: 1;
  }

  .message-content p {
    margin: 0 0 0.5rem 0;
    color: var(--dark);
    font-size: 0.875rem;
    line-height: 1.5;
  }

  .course-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .course-list li {
    padding: 0.375rem 0;
    font-size: 0.813rem;
    color: var(--gray);
    animation: fadeIn 0.5s ease-out backwards;
  }

  .course-list li:nth-child(1) { animation-delay: 0.2s; }
  .course-list li:nth-child(2) { animation-delay: 0.4s; }
  .course-list li:nth-child(3) { animation-delay: 0.6s; }
  .course-list li:nth-child(4) { animation-delay: 0.8s; }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  .typing {
    color: var(--dark);
  }

  .cursor {
    animation: blink 1s ease-in-out infinite;
    color: var(--primary);
    font-weight: 400;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .chat-input {
    display: flex;
    gap: 0.5rem;
    padding: 1rem 1.5rem;
    border-top: 1px solid #e5e7eb;
    background: #fafafa;
  }

  .chat-input input {
    flex: 1;
    padding: 0.5rem 1rem;
    border: 1px solid #e5e7eb;
    border-radius: 0.5rem;
    font-size: 0.875rem;
    background: var(--white);
  }

  .chat-input input::placeholder {
    color: #9ca3af;
  }

  .send-btn {
    padding: 0.5rem 1rem;
    background: var(--primary);
    color: var(--white);
    border: none;
    border-radius: 0.5rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s ease;
  }

  .send-btn:hover {
    background: var(--primary-dark);
    transform: translateY(-1px);
  }

  @media (max-width: 767px) {
    .hero-badge {
      font-size: 0.75rem;
      gap: 0.5rem;
    }

    .hero-stats {
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      padding: 1rem;
    }

    .stat-value {
      font-size: 1.5rem;
    }
  }
</style>