---

---

<section class="hero section">
  <div class="container">
    <div class="hero-content">
      <div class="hero-badge">
        <span>⚡ Без кода</span>
        <span>⚙️ API и облако</span>
        <span>💳 Оплата картой РФ</span>
      </div>
      
      <h1 class="hero-title">
        Единая AI-платформа для бизнеса и авторов. 
        <span class="text-gradient">Оплата картой РФ.</span>
      </h1>
      
      <p class="hero-subtitle">
        Доступ к передовым нейросетям, запуск ИИ-агентов и коробочное решение для создания курсов за минуты.
      </p>
      
      <div class="hero-actions">
        <a href="#lead-form" class="btn btn-primary btn-large" data-form-type="b2c">Попробовать бесплатно</a>
        <a href="#lead-form" class="btn btn-secondary btn-large" data-form-type="b2b">Запросить демо</a>
      </div>
      
      <div class="hero-stats">
        <div class="stat">
          <div class="stat-value">50+</div>
          <div class="stat-label">AI моделей</div>
        </div>
        <div class="stat">
          <div class="stat-value">Без VPN</div>
          <div class="stat-label">Работает в РФ</div>
        </div>
        <div class="stat">
          <div class="stat-value">24/7</div>
          <div class="stat-label">Поддержка</div>
        </div>
      </div>
    </div>
    
    <div class="hero-visual">
      <div class="visual-placeholder">
        <div class="visual-card">
          <div class="card-header">
            <span class="card-badge">Генерация курса</span>
            <span class="card-status">В процессе...</span>
          </div>
          <div class="card-content">
            <div class="generation-item completed">
              <span class="check-icon">✓</span>
              <div class="item-text">
                <strong>Анализ аудитории</strong>
                <span>Предприниматели 25-45 лет</span>
              </div>
            </div>
            <div class="generation-item completed">
              <span class="check-icon">✓</span>
              <div class="item-text">
                <strong>Тема курса</strong>
                <span>AI для автоматизации бизнеса</span>
              </div>
            </div>
            <div class="generation-item active">
              <span class="loader-icon">⟳</span>
              <div class="item-text">
                <strong>Создание контента</strong>
                <span>Модуль 3 из 7...</span>
              </div>
            </div>
            <div class="generation-item pending">
              <span class="pending-icon">○</span>
              <div class="item-text">
                <strong>Генерация видео</strong>
                <span>Ожидание</span>
              </div>
            </div>
          </div>
          <div class="card-footer">
            <div class="progress-bar">
              <div class="progress-fill"></div>
            </div>
            <span class="progress-text">62% завершено</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(180deg, #f9fafb 0%, rgba(249, 250, 251, 0) 100%);
    overflow: hidden;
    position: relative;
  }

  .hero-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero-badge {
    display: inline-flex;
    gap: 1rem;
    padding: 0.5rem 1rem;
    background: var(--white);
    border: 1px solid #e5e7eb;
    border-radius: 2rem;
    margin-bottom: 2rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--gray);
  }

  .hero-badge span {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .hero-title {
    margin-bottom: 1.5rem;
    font-weight: 800;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--gray);
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 3rem;
  }

  .btn-large {
    padding: 1rem 2rem;
    font-size: 1.125rem;
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    padding: 2rem;
    background: var(--white);
    border-radius: 1rem;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
    margin-top: 3rem;
  }

  .stat {
    text-align: center;
  }

  .stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--primary);
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--gray);
    margin-top: 0.25rem;
  }

  .hero-visual {
    margin-top: 4rem;
    position: relative;
  }

  .visual-placeholder {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 1rem;
    padding: 2rem;
    min-height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .visual-card {
    background: var(--white);
    border-radius: 0.75rem;
    padding: 1.5rem;
    width: 100%;
    max-width: 450px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
  }

  .card-badge {
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: var(--white);
    padding: 0.25rem 0.75rem;
    border-radius: 1rem;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .card-status {
    color: var(--secondary);
    font-size: 0.875rem;
    animation: blink 2s ease-in-out infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .generation-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.75rem;
    border-radius: 0.5rem;
    margin-bottom: 0.5rem;
    transition: all 0.3s ease;
  }

  .generation-item.completed {
    background: #f0fdf4;
  }

  .generation-item.active {
    background: #fef3c7;
    animation: activeGlow 2s ease-in-out infinite;
  }

  @keyframes activeGlow {
    0%, 100% { box-shadow: 0 0 0 0 rgba(245, 158, 11, 0); }
    50% { box-shadow: 0 0 10px 2px rgba(245, 158, 11, 0.2); }
  }

  .generation-item.pending {
    opacity: 0.6;
  }

  .check-icon {
    color: var(--secondary);
    font-size: 1.25rem;
    font-weight: bold;
  }

  .loader-icon {
    color: var(--accent);
    font-size: 1.25rem;
    animation: rotate 1s linear infinite;
  }

  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .pending-icon {
    color: #d1d5db;
    font-size: 1.25rem;
  }

  .item-text {
    flex: 1;
  }

  .item-text strong {
    display: block;
    color: var(--dark);
    font-size: 0.875rem;
    margin-bottom: 0.25rem;
  }

  .item-text span {
    color: var(--gray);
    font-size: 0.75rem;
  }

  .card-footer {
    margin-top: 1.5rem;
    padding-top: 1rem;
    border-top: 1px solid #e5e7eb;
  }

  .progress-bar {
    height: 6px;
    background: #e5e7eb;
    border-radius: 3px;
    overflow: hidden;
    margin-bottom: 0.5rem;
  }

  .progress-fill {
    height: 100%;
    width: 62%;
    background: linear-gradient(90deg, var(--primary) 0%, var(--secondary) 100%);
    border-radius: 3px;
    animation: progressGrow 2s ease-out;
  }

  @keyframes progressGrow {
    from { width: 0; }
    to { width: 62%; }
  }

  .progress-text {
    font-size: 0.75rem;
    color: var(--gray);
    display: block;
    text-align: center;
  }

  @media (max-width: 767px) {
    .hero-badge {
      font-size: 0.75rem;
      gap: 0.5rem;
    }

    .hero-stats {
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      padding: 1rem;
    }

    .stat-value {
      font-size: 1.5rem;
    }
  }
</style>